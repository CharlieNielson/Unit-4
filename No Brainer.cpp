#include <iostream>
using namespace std;

// A class is an object you can define, and a method is a function that is part of a class.

class example {
    public:
    // Actions go here.
    example();// Constructor
    private:
    // Properties go here.
}

example::example() {/*scope resolution operator is ::*/
    // Property definitions.
}
